Bitwise XOR(^) operator can be used to find a single number in an array of numbers repeated twice.

To get Quotient:

```
Math.floor(a/b) 
(a/b)>>0       //right shift operator
~~(a/b)        // ~ ~ operator
```

Sign

```
Math.sign(-34)  => -1
Math.sign(28)   => 1
```